<form action="{{ path('register') }}" {{ form_enctype(form) }} method="POST">
    {{ form_widget(form) }}
    
    <button type="submit" >
        Register
    </button>
    
    <a href="{{ path('twitter_register') }}">Register with Twitter</a>
</form>